---
description: Rooms, the shared memory your agents read. Start, join, create, invite, accept, leave, kick
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/bin/crosstalk:*)
---

!`"${CLAUDE_PLUGIN_ROOT}/bin/crosstalk" cli room $ARGUMENTS`
